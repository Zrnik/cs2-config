// To initialize, we have to 1st call volume to reset it
exec "custom_cfg/volume";

exec "custom_cfg/walk/enable"; // Raises volume
exec "custom_cfg/walk/disable"; // Resets it back to normal, binds message mode
